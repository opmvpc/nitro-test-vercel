{
  "name": "example-hello-world",
  "private": true,
  "scripts": {
    "dev": "concurrently \"nitro dev\" \"cd front && vite\"",
    "build": "nitro build && cd front && vite build"
  },
  "devDependencies": {
    "nitropack": "latest",
    "concurrently": "^8.2.1"
  }
}
